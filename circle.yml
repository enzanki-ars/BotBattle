general:
  artifacts: 
    - "build/launch4j"
    - "build/distributions"

test:
  override:
    - gradle -q versionPrint
    - TERM=dumb ./gradlew test
  post:
    - TERM=dumb ./gradlew distZip distTar
    - TERM=dumb ./gradlew launch4j
