general:
  artifacts: 
    - "launch4j"
    - "build/distributions"

test:
  override:
    - TERM=dumb gradle test
  post:
    - TERM=dumb ./gradlew distZip distTar
    - TERM=dumb ./gradlew launch4j
