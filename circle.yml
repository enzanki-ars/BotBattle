general:
  artifacts: 
    - "build"
    - "build/launch4j"
    - "build/distributions"

test:
  override:
    - TERM=dumb ./gradlew test
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex ".*/build/test-results/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \;
  post:
    - TERM=dumb ./gradlew distZip distTar
    - TERM=dumb ./gradlew launch4j
